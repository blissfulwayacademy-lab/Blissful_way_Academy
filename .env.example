# Supabase — Project Settings → API
# Copy this file to .env and fill in both values. Restart the dev server after editing.
# Both are exposed to the browser via the VITE_ prefix. Never put the service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
